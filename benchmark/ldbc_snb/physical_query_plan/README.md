# LDBC SNB Physical MongoDB Query-Plan Validation

This folder documents the physical MongoDB query-plan validation for the LDBC SNB workload used in the SchemaLens evaluation.

The experiment validates the physical MongoDB materializations for the LDBC SNB candidates at SF0.1, SF1, and SF3. The benchmark latency analysis covers the official IC, IS, and INS workloads. The query-plan analysis covers the read-style workload components, namely IC1--IC7 and IS1--IS7, using MongoDB `explain("executionStats")`.

Insert workloads INS1--INS8 are evaluated through benchmark latency and write-execution evidence. They are not reported in the read-style query-plan summary because MongoDB does not expose the same `executionStats` structure for write operations as it does for `find` and `aggregate` plans.

## Scope

* Read queries with detailed query-plan evidence: IC1--IC7 and IS1--IS7.
* Write queries included in latency evaluation: INS1--INS8.
* Query-plan metrics: physical operators, documents examined, keys examined, index usage, disk usage, and stage-level evidence.
* Scale factors: SF0.1, SF1, and SF3.
* MongoDB access-path validation: all read-query plans are indexed; no read-query plan uses `COLLSCAN`.

IC7 required chunking large `$in` predicates during auxiliary materialization to avoid MongoDB's 16 MB BSON command limit. This preserves the full materialized data and does not reduce the benchmark dataset.

## Main scripts

The physical query-plan consolidation is generated by:

```bash
python analysis/scripts/analyze_ldbc_snb_physical_query_plan.py
```

The representative-case baseline and ablation analysis for IC7 is generated by:

```bash
python analysis/scripts/recompute_ldbc_physical_ic7_baseline_ablation.py
```

## Main generated files

The consolidated outputs are stored in:

```text
analysis/generated/ldbc_snb_physical_query_plan/
```

Main files:

```text
ldbc_snb_physical_benchmark_query_plan_joined.csv
ldbc_snb_physical_hot_winners_with_query_plan.csv
ldbc_snb_physical_query_plan_candidate_summary.csv
ldbc_snb_physical_query_plan_component_results.csv
ldbc_snb_physical_query_plan_coverage.csv
ldbc_snb_physical_query_plan_report.md
ldbc_snb_physical_query_plan_scale_summary.csv
ldbc_snb_physical_ic7_baseline_behavior.csv
ldbc_snb_physical_ic7_ablation_behavior.csv
ldbc_snb_physical_ic7_baseline_ablation_tables.tex
```

## Physical LDBC SNB summary

The physical MongoDB validation covers three scale factors: SF0.1, SF1, and SF3.

Hot-phase benchmark summary:

| Scale | Queries | Activated Top-1 | Mean activated regret | Mean primary regret | Primary wins | Secondary wins | Control wins |
| ----- | ------: | --------------: | --------------------: | ------------------: | -----------: | -------------: | -----------: |
| SF0.1 |      22 |          0.9545 |              0.000006 |              0.0651 |           17 |              4 |            1 |
| SF1   |      22 |          0.9545 |              0.001898 |              0.0521 |           14 |              7 |            1 |
| SF3   |      22 |          0.9545 |              0.001473 |              0.0411 |           14 |              7 |            1 |

Overall, SchemaLens preserves the global Top-1 configuration in 63 out of 66 hot query-scale cases, with mean activated regret close to zero.

Read-query plan summary:

| Scale | Candidate-phase rows | Components | Docs examined | Keys examined | IXSCAN rows | COLLSCAN rows | SORT rows |
| ----- | -------------------: | ---------: | ------------: | ------------: | ----------: | ------------: | --------: |
| SF0.1 |                  128 |        316 |        15,084 |        15,248 |          76 |             0 |        26 |
| SF1   |                  128 |        316 |        41,044 |        41,224 |          76 |             0 |        26 |
| SF3   |                  128 |        316 |        85,654 |        85,894 |          76 |             0 |        26 |

Final coverage result:

* All read-query plans for IC1--IC7 and IS1--IS7 are available.
* INS1--INS8 are evaluated through latency and write-execution evidence.
* No read-query plan uses `COLLSCAN` across SF0.1, SF1, and SF3.

## Representative case: IC7

IC7 (Recent Likers) is used as the representative LDBC SNB physical query-plan case because it combines a person-rooted access path with associative relationships over likes and person-to-person relationships.

The measured physical SchemaLens space for IC7 is:

```text
G0, G3, G4, G6
```

Physical hot-phase winners:

| Scale | Winner |   p95 ms | Candidate role                                           |
| ----- | ------ | -------: | -------------------------------------------------------- |
| SF0.1 | G3     | 0.780906 | Primary person-rooted candidate                          |
| SF1   | G6     | 1.172215 | Secondary reverse-reference / endpoint-indexed candidate |
| SF3   | G3     | 0.858621 | Primary person-rooted candidate                          |

The IC7 baseline and ablation outputs are:

```text
ldbc_snb_physical_ic7_baseline_behavior.csv
ldbc_snb_physical_ic7_ablation_behavior.csv
ldbc_snb_physical_ic7_baseline_ablation_tables.tex
```

Main interpretation:

* SchemaLens preserves Top-1 in all three IC7 physical scales.
* random-k is reported as a budget-matched statistical control.
* always-embed misses the winner at all three scales.
* always-reference, depth-only, and relationship-type-only miss the SF1 winner because they do not preserve G6.
* The ablation results show that relationship semantics, depth, residual traversal, sharedness, and update-sensitive evidence contribute to preserving the correct IC7 family members.

## Reproduction

From the repository root, run:

```bash
python analysis/scripts/analyze_ldbc_snb_physical_query_plan.py
python analysis/scripts/recompute_ldbc_physical_ic7_baseline_ablation.py
```

Then check the generated outputs:

```bash
ls -lh analysis/generated/ldbc_snb_physical_query_plan/
```

## Git tracking note

The generated CSV, Markdown, and LaTeX summary files under `analysis/generated/ldbc_snb_physical_query_plan/` are lightweight analysis artifacts and can be tracked.

The raw per-query benchmark folders under `benchmark/ldbc_snb/physical_benchmark/` can be large and should only be tracked selectively when required by the artifact policy.
